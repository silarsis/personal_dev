# Registry, so we keep local copies of things to save on network traffic
registry:
  container:
    Image: registry
    Hostname: registry
    ExposedPorts:
      5000/tcp: {}
  run:
    portBindings:
      5000/tcp:
        -
          HostPort: "5000"